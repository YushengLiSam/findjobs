# 排障库 — 问题 → 解法

**只增不删。** 每次投递遇到卡住的地方，无论最后解没解决，都追加一条。
再遇到同样症状时先查这里，不要重新排查。

格式：`症状` → `原因` → `解法`。症状要写**当时看到的现象**（而不是事后才知道的原因），这样下次才搜得到。

## ⚠️ 追加协议（多个 agent 会同时写这个文件）

1. **新条目用日期 slug 当 ID，不要用顺序号** —— `### YYYY-MM-DD-英文slug · 症状`。
   顺序号会撞：Claude 和 Codex 各自追加、互不知情，2026-09-05 已经撞出两对重号。
2. **只在文件末尾 append**，写之前先 `grep '^### ' troubleshooting.md | tail -3` 看最新几条。
3. 老条目的数字编号（#1~#33）保留不动，`_ats-tactics.md` 等文件还在引用它们。
4. **标注适用的 agent**：条目里若出现某个 agent 专属的工具名，开头加 `[claude]` / `[codex]` / `[通用]`。
   术语对照见 `platforms/_ats-tactics.md` 顶部。

---

## 环境与接入

### 1. 用户说"我已经登录了"，但页面显示未登录

**原因**：用户在自己的 Chrome 登录，而我用的是 Claude 内置浏览器（`mcp__Claude_Browser__*`），两者 cookie 完全独立。

**解法**：改用 Claude in Chrome（`mcp__claude-in-chrome__*`）接管用户真实 Chrome。先 `list_connected_browsers` 确认有连接。**投递类任务一律用真实 Chrome，不要用内置浏览器。**

### 2. `list_connected_browsers` 返回空数组 `[]`

**原因**：Claude in Chrome 扩展没装或没连上。

**解法**：让用户装 [Chrome Web Store 的 Claude 扩展](https://chromewebstore.google.com/detail/claude/fcoeoabgfenejglbffodgkkbkcdhcgfn)（认准发布者 Anthropic），装完**重启 Chrome**（首次安装要读 native messaging 配置文件）。需要付费计划且 Claude Code 是 `/login` 登录的（API key / setup-token 认证会强制关闭该集成）。

### 3. 内置浏览器传不了文件

**原因**：`mcp__Claude_Browser__*` 没有 `file_upload` 工具，只有 claude-in-chrome 有。

**解法**：同上，用真实 Chrome。另外**不要去点上传按钮**——会弹出系统原生文件选择器，看不见也点不了；要用 `file_upload` + 文件输入框的 ref。

### 4. 浏览器面板隐藏时 `computer` 操作 30 秒超时

**原因**：面板被隐藏，截图/点击类操作拿不到渲染。

**解法**：改用 `javascript_tool` 读取状态（不依赖可见性），或让用户把面板调出来。

---

## 表单交互

> 以下几条的完整版见 [`platforms/_ats-tactics.md`](platforms/_ats-tactics.md)，这里只留速查。

### 5. 点了"保存"，改动却全部消失，页面退回展示态且无报错

**原因**：点到了旁边的「取消」。JS `getBoundingClientRect()` 是 CSS 像素，`computer` 的坐标系是截图像素，差约 1.05 倍；在 x≈1264 处偏 60px 正好从保存落到取消。

**解法**：`截图坐标 = CSS坐标 × (截图宽 / window.innerWidth)`。或更稳妥：**截图后肉眼读坐标再点**。点保存前必截图确认两个按钮位置。

### 6. 下拉选项点了但选中的是相邻项

**原因**：列表在"算坐标"和"点击"之间重排/滚动。名字互相包含时尤其危险（搜"哥伦比亚大学"会命中两个 UBC，精确项排最后）。

**解法**：改键盘法——`输入收窄 → Down/Up → 用 JS 读回 active class 确认高亮在目标项 → Enter → 再读回控件显示值确认`。第三步不能省。

### 7. 以为下拉没打开，DOM 里查不到选项

**原因**：页面混用多套 UI 库，下拉渲染在不同容器。

**解法**：一网打尽查 `[...document.querySelectorAll('[class*=dropdown]')].filter(d=>!d.className.includes('hidden'))`。Ant Design 用 `.ant-select-dropdown`，rocket-select 用 `[class*=rocket-select-dropdown]`。

### 8. 日期字段打字无效，还被静默设成当前月份

**原因**：日期控件不接受文本输入，`form_input` 对它也无效（会设成空）。

**解法**：走面板 —— `点字段 → 面板打开 → 点 «/» 翻年 → 截图确认年份 → 点月份`。

### 9. 点了保存没反应，区块仍停在编辑态，也没有报错

**原因**：选某个值解锁了新的必填项，保存静默失败，只是把新字段显示出来。

**解法**：**每次保存后检查页面文本里还有没有「取消」**。有就是还没保存成功，往下找新出现的必填字段。已知链条见 `platforms/pdd-campus.md`。

### 10. 点其他标签页完全没反应

**原因**：当前区块处于编辑态，锁住了标签切换。

**解法**：先保存当前区块。

### 11. 长文本字段读不到内容，以为丢了

**原因**：展示态折叠成 `... 展开`，`innerText` 读不到。

**解法**：先点「展开」再读。

---

## 数据正确性

### 12. 账号里的已存资料是过期的

**症状**：最高学历还是本科、毕业学校是本科母校、邮箱是已停用的学校邮箱、自我评价结尾写着"这次**实习**机会"。

**原因**：平台账号存的是**上一次投递时**的资料，可能过期一整个学历阶段。

**解法**：**进表单第一件事是逐字段读一遍和简历比对**，不要假设账号里有的就是对的。发现冲突时先报告用户再改（涉及学历、学校、联系方式这类身份事实）。

### 13. 必填下拉里没有任何一个选项符合真实情况

**症状**：如「相关证书」只列出 CATTI/专四专八，而候选人一个都没有。

**原因**：列表是服务器搜索的，默认只显示前 N 项。

**解法**：**在搜索框里搜关键词**——搜「无」通常能找到「无相关证书」，搜「托福」出分数段。
**绝不因为"必填"就编造一个成绩或证书。** 找不到合适选项就问用户。

---

### 14. 点了选项，JS 查 `input.checked` 全是 `false`（问卷星）

**症状**：肉眼看页面上选项明明是选中的，代码校验却报"未选中"，白白重填一轮。

**原因**：问卷星**不维护 `input.checked`**。选中状态记在外层 `div.ui-radio` / `div.ui-checkbox` 的 **`checked` class** 上。

**解法**：校验查 `#div<N> .ui-radio.checked .label`。另外**单选** `input.click()` 有效，**多选**必须点外层 `.ui-checkbox`——两者绑定方式不同，别假设一致。详见 `platforms/wjx-questionnaire.md`。

### 15. `input[type=text]` 选择器一个元素都匹配不到，但页面上明明全是文本框

**原因**：很多 React 表单（得物用的 Formily）渲染出来的 `<input>` **没有显式 `type` 属性**，属性选择器自然匹配不上。

**解法**：改用 DOM 属性而不是属性选择器过滤——`[...el.querySelectorAll('input')].filter(e => e.type === 'text')`。`.type` 属性会返回默认值 `text`，`[type=text]` 选择器不会。

### 16. 下拉点了选项，填进去的却是另一个字段的候选值

**症状**：明明点的是「渠道」的下拉，结果控件显示的是学历或城市里的某一项。

**原因**：Formily 的下拉弹层**打开过就一直留在 DOM 里**，不会销毁。按"哪个可见"（opacity / display）去挑，很容易挑到上一个字段的过期弹层。

**解法**：**按选项内容签名定位弹层**，不要按可见性或顺序：
```js
const dd = [...document.querySelectorAll('[class*="select__dropdown"]')]
  .find(e => [...e.querySelectorAll('[class*="select__list__item"]')]
    .some(x => x.innerText.trim() === '目标选项'));
```
选完再读回该字段的 `[class*="control-content"]` 文本确认。

### 17. 新增一段子表单后，之前填好的值跑到了错误的字段里

**症状**：加了第二段项目经历，回头一看项目名写进了「项目角色」，角色写进了「起止时间」。

**原因**：按 `.ud-formily-item` 的**数组索引**定位字段。加一段子表单，后面所有索引整体偏移。

**解法**：**按 label 文本定位，永远不要缓存索引**：
```js
const findByLabel = kw => items.findIndex(it =>
  (it.querySelector('[class*="formily-item-label"]')?.innerText || '').includes(kw));
```
已经写错位的日期字段要**先清空再重填**，残留值不会被覆盖。

### 18. Claude in Chrome 导航报 "This site is not allowed due to safety restrictions"

**症状**：`navigate` 直接失败，不是超时也不是权限弹窗，域名根路径同样被拒。

**原因**：该域名在扩展的**策略黑名单**上，属于域名级封锁，不是登录态或权限问题。已知被封：`join.qq.com`（腾讯招聘）。

**解法**：**没有绕法，也不要尝试绕**（换内置浏览器同样不解决，且内置浏览器没登录态）。改为人工协作模式：让用户把表单页面文字复制过来（或截图），我按知识库产出「字段 → 填什么」的对照清单，用户自己粘贴。自由文本（自我评价、项目描述）照常由我生成，这部分本来就是价值大头。

### 19. 用 \u 转义写中文，打错了字

**症状**：给「姓名」填 `\u674e\u96e8\u6668`，页面显示「李雨**晨**」——正确的是「李雨**晟**」（晟=\u665f）。
名字打错在简历上是硬伤，而且我是靠事后截图放大才发现的。

**原因**：手写 Unicode 转义时挑错码点。形近字（晟/晨、辛/星）尤其危险。

**解法**：**工具调用里直接写字面中文，不要用 \u 转义**。涉及姓名、学校名这类专有名词，
填完必须 `zoom` 放大截图逐字核对，别只看"填进去了"。

### 20. 表单填完刷新后，只有一部分值还在

**症状**：顶层字段（所在地、证件号）刷新后仍在，子表单里的（学校、专业、起止时间）全空了。

**原因**：用 `form_input` / React 原生 setter 灌进**下拉型组件**的值，只改了 DOM 显示，
没进组件状态，因此不会随保存入库；纯文本 `<input>` 则能正常入库。

**解法**：**保存后刷新页面，逐字段读回，以刷新后的结果为准。**
下拉必须走真实 UI（点开→打字过滤→点选项→读回）。判断是不是下拉：看有没有 ▼，或点一下看有没有候选浮层。

### 21. `type` 报 CDP 超时，但内容其实已经输入了

**症状**：打长文本时报 `Input.insertText timed out after 30000ms`，看起来失败了。

**原因**：页面重渲染卡住了返回通道，输入本身已经完成。

**解法**：**不要重试**（会打成两遍）。先用 JS 查 `textarea.value.length` 确认，再决定要不要补。
同理，`computer` 截图报 `Script injection timed out` 时 `javascript_tool` 往往还能用，可以拿它继续读状态，等十几秒截图会自己恢复。

### 22. `scrollTo` / `scrollIntoView` 无效，算出来的坐标全在视口外

**症状**：滚到某字段后读 `getBoundingClientRect()`，top 是 1800+，远超视口高度；点击自然全落空。

**原因**：页面有滚动监听组件（右侧锚点导航）会把程序化滚动**重置回原位**。

**解法**：改用 `computer` 的 `scroll`（真实滚轮事件），约 136px/tick，滚完**截图确认位置再读坐标**。

### 23. Chrome 简历上传时报 `fileChooser.setFiles failed: Not allowed`

**症状**：招聘表单其余字段可以正常填写，但给文件选择器设置本地简历时立刻返回 `Not allowed`。

**原因**：ChatGPT 浏览器扩展未获准访问本地 `file://` URL；这不是简历路径错误，也不是招聘网站拒绝文件。

**解法**：打开 `chrome://extensions` → 找到 ChatGPT 浏览器扩展 → 点击 **Details** → 启用 **Allow access to file URLs**，然后回到原表单重试上传。不要为解决此问题刷新表单；先保留已填内容和当前标签页。

### 24. 强制点击隐藏的文件 input，等待文件选择器超时

**症状**：页面明明有 `input[type=file]`，对隐藏 input 使用强制点击后仍报等待 file chooser 超时；点击页面可见的“选择文件”则能触发 chooser。

**原因**：部分 Ant Design 上传组件只在可见按钮/上传容器的事件链上真正触发文件选择器，直接点击其 `display:none` 的 input 不会触发扩展可捕获的 chooser。

**解法**：先启动 `waitForEvent("filechooser")`，再点击可见的“选择文件”按钮；拿到 chooser 后调用 `setFiles`。若随后报 `Not allowed`，按第 23 条开启扩展的本地文件 URL 权限。

### 25. 勾选“至今”后，后续日期字段的索引突然变化

**症状**：给某段项目勾选“至今”后，原本存在的结束日期 input 从 DOM 消失，后续按全页 `nth()` 访问日期时命中错误字段或超时。

**原因**：B站招聘表单会用一个禁用的“至今”展示框替换原结束月份 input，导致全页 `input[placeholder="请选择日期"]` 数量减少。

**解法**：每次新增/勾选后重新查询；优先把日期定位限制在当前经历卡片内，不跨操作缓存全页索引。

### 26. Chrome 已运行、扩展已启用且连接配置正常，但所有标签页调用持续超时

**症状**：浏览器选择成功，但 `nameSession`、新建标签页或列出标签页连续超时并重置连接；诊断显示 Chrome 正在运行、ChatGPT 扩展已启用、原生连接配置正确。

**原因**：当前 Chrome 资料窗口与扩展控制通道卡住；不是扩展缺失或原生配置错误。

**解法**：先征得用户同意，再用浏览器插件自带的恢复脚本为选中资料打开一个新 Chrome 窗口，等待两秒后只重试一次。仍失败则让用户从 ChatGPT 插件界面重新安装 Browser 插件；不要自行修复原生配置，也不要改用 AppleScript 或 shell 自动化网页。

### 27. 米哈游哈希岗位页地址正确，但应用内页面长期只有空白 document

**症状**：打开 `jobs.mihoyo.com/#/campus/position/<id>` 后 URL 和标题正确，但 DOM 只有 `document`，导航或截图超时，控制台没有明确错误。

**原因**：米哈游招聘站是按路由动态加载的 SPA；应用内浏览器在该次会话中没有完成前端渲染。

**解法**：只读核对 JD 时，可调用官网前端实际使用的官方接口：`POST https://ats.openout.mihoyo.com/ats-portal/v1/job/info`，校招参数为 `id`、`channelDetailIds:[1]`、`hireType:1`，并从官网 HTML 核对当日 `Release-Tag`。这只解决岗位信息读取；登录、简历上传和投递仍需浏览器。空白并非永久故障：本次后续新应用内浏览器会话等待约 2 秒后成功渲染了岗位；若重开一次仍空白再用接口，不要无限重试。

### 28. Moka 招聘首页用 curl 访问时反复 302 到同一个 URL

**症状**：`app.mokahr.com/campus-recruitment/...` 连续重定向到自身，看起来像页面或链接失效。

**原因**：首次响应会设置 Moka 会话 cookie；不保留 cookie 的后续请求会被当成新的首次访问，再次跳转。

**解法**：用同一个 cookie jar 完成首次页面访问，并在岗位详情、申请表等官方公开接口请求中复用。岗位详情有时使用官网前端内置的 AES-CBC 响应封装，公开解密参数和实现都在页面数据与当前 JS bundle 中；仅用于只读核对公开 JD/表单结构，不能替代登录、验证码或最终提交。

### 29. 外部 Chrome 控制失效时，应用内浏览器能打开表单但没有登录态

**症状**：Chrome 扩展控制持续超时；切到应用内浏览器后岗位和申请表可正常渲染，但招聘站立即弹手机号/邮箱/微信登录。

**原因**：应用内浏览器与用户 Chrome 使用不同会话，不会自动继承招聘网站账号。

**解法**：可先在应用内浏览器完成只读字段核对；需要继续填写时，让用户参与一次验证码或扫码登录。不要读取、迁移或复制 Chrome 的 cookie/会话存储，也不要把公开接口当成绕过登录的提交通道。

### 30. 飞书招聘申请链接重定向到手机号登录，且区号默认 +1

**症状**：打开 `*.jobs.feishu.cn/<site>/resume/<position_id>/apply` 后进入登录页；中国手机号已填，但页面国家区号仍是 `+1`，继续获取验证码会发往错误号码或校验失败。

**原因**：申请页要求先建立招聘站账号；登录页会按当前浏览器环境默认国家区号，应用内浏览器可能默认为美国 `+1`。

**解法**：先精确选择“中国大陆 +86”，再填写手机号并读回核对。停在“同意并阅读《隐私声明》/获取验证码”前让用户参与：由用户确认条款并触发短信，验证码由用户提供后继续。保持申请标签页打开，不要刷新，也不要尝试读取或迁移其他浏览器的登录态。

### 31. 北森 zhiye 申请页重定向扫码登录，DOM 快照却为空

**症状**：打开 `*.zhiye.com/form?jobAdId=...` 后跳转登录页；页面标题正常，但可访问性 DOM 快照为空，看起来像白屏。

**原因**：北森门户已渲染，但该登录模板没有暴露可访问性树；实际可见内容是“微信扫码登录/注册”、北森隐私政策复选框和二维码。未登录的申请表数据接口会返回 `401 NO AUTH`。

**解法**：先只读当前 DOM 的可见文本确认页面确实已渲染，再把登录标签页交给用户：由用户勾选隐私政策并微信扫码。扫码完成后重新读取实际表单；不要用通用北森字段模板替代租户表单，也不要绕过登录接口。

**复发环境提示**：若真实 Chrome 的扩展、原生连接和运行状态检查都正常，但列标签页仍连续超时，按第26条处理；未经用户许可不要代开新的 Chrome 窗口。应用内浏览器仍可保留扫码登录页供用户接管。

### 32. 已接管的 Chrome 标签页在控制会话重置后，重新 claim 持续超时

**症状**：北森表单已经在用户 Chrome 中登录并填写了一部分；浏览器控制会话意外重置后，`openTabs()` 能正常列出该页，但重新接管同一个用户标签页持续超时。

**原因**：旧标签页仍残留在上一控制会话的接管状态，新的会话无法及时重新取得控制；这不代表 Chrome 登录态丢失。

**解法**：不要反复 claim 同一标签页。通过同一 Chrome 连接新建受控标签页并直接打开相同申请 URL；新页会共享登录态。重新核对岗位后再按知识库回填，保留原用户标签页作为未保存内容的备用，最终只把新的完整表单页交给用户。

### 33. 北森表单留在页面里看似已填，重新打开后部分教育字段丢失

**症状**：摩尔线程北森申请表中两段教育在当前页面读回完整；控制会话结束或页面重新加载后，学校和起止时间仍在，但 GPA、专业排名及部分学院字段为空或回到旧值。

**原因**：当前页面中的输入尚未通过底部“暂存”写入服务器；保留标签页不等于保存草稿，账号旧资料与本次临时页面状态会混在一起。

**解法**：完成所有非上传字段后立即点“暂存”，重新打开申请页逐字段核对；确认教育、经历和自由文本都持久化后再交给用户预览并提交。若用户已提交成功，停止修改并如实记录提交前未持久化的字段，不能声称最终提交内容完整。

---

## 待补充

（下次遇到新问题追加在这里，验证有普适性后再提升进 `_ats-tactics.md`）

### 2026-08-29-aggregator-fake-jobname · 聚合站上的岗位名是它自己归纳的，官方根本没这个岗

**症状**：superlinear/wondercv/quanzhi/hicv 这类校招聚合站列出「前线部署工程师（FDE）」，
按这个名字去官方站搜，一条都搜不到。截止日期同理——聚合站写 2027-02-08，官方 API 的 closeTime 是 null。

**原因**：聚合站会把公司公告里的岗位**归类改写**成更好懂的名字，不是照抄官方岗位名。
截止日也常是它自己推的。**这类页面只能证明「这家开了 27 届校招」，不能证明「有这个岗」。**

**解法**：
1. **岗位名和截止日必须以官方站为准**，聚合站只当"哪家开了"的线索。
2. 很多招聘站有**公开 JSON API**，比抓 SPA 页面可靠得多，能一次拉全量岗位做正则匹配。
   已验证：快手 `POST https://campus.kuaishou.cn/recruit/campus/e/api/v1/open/positions/simple`
   （批次 code 从字典 API 取，2027应届生=20271779425607）。
   遇到 SPA 抓不到列表时，**先看 Network 里有没有这种接口**。
3. 教训：`hunting/README.md` 2.5 节的 D 级链接规则就是为这个立的——
   D 级页面的**内容**同样不可信，不只是链接不能交。


### 2026-09-05-date-rot · 日期算错：把「今天」烤进了数据，隔几天全错

**症状**：给用户的倒计时说「剩 17 天」，实际只剩 10 天。生成时的 TODAY 是 2026-08-29，
但用户是 09-05 才打开看的——会话跨天恢复、artifact 长期存在，都会让这个差越拉越大。

**原因**：两个错叠在一起。
1. **没跑 `date` 就假设今天几号。** 系统提示里其实写了正确日期，我用了自己推的。
2. **把算好的天数写死进数据**（`days: 17`），而不是存截止日让展示端实时算。

**解法**：
1. **任何涉及日期的操作，先跑 `date "+%Y-%m-%d"`**，不要凭上下文推断。
   这条已经犯过两次（上次是 session-log 日期写错，见回写清单）。
2. **存事实，不存推导值。** 数据里存 `iso: "2026-09-15"`，天数在渲染时 `new Date()` 现算。
   一切「相对现在」的量（剩几天/几天前/是否过期）都不能预计算。
3. **长期存在的产物要自带陈旧提醒**：把生成日期存进去，打开时算 staleness，
   超过阈值就在页面上挂提示，别让用户以为看到的是最新的。

**同类风险还没修的**：`job-leads.md` 的排除名单、`hunting/README.md` 的「截至X月X日已投」
都是手写快照，应该从 `session-log/` 现算。


### 2026-09-05-beisen-vivo-intent-not-saved · [通用] 北森「暂存」不保存投递意向段，刷新后回到「请选择」

**症状**：vivo（北森 hr-campus.vivo.com）填完整表点「暂存」，提示"暂存成功"。
刷新后个人信息、教育经历、工作经历、项目经历、自我评价**全部保住**，
唯独顶部「投递意向」的**面试站点**和**意向工作地点**回到「请选择」。

**原因**：北森把表单分成两层——**候选人档案**（跨岗位复用，暂存会入库）和
**本次投递的意向**（岗位维度，只有真正提交时才写）。暂存只保存前者。

**解法**：
1. 暂存后**必须刷新并逐字段读回**（这条 `platforms/beisen-zhiye.md` 早写了，这次验证到了新的一类）。
2. 「投递意向」段**在最终提交前重新选一遍**，选完**别关标签页**。
3. 判断哪些字段属于这一层：看它是不是岗位相关（面试站点/意向地点/第二志愿/推荐码），
   这些多半不入库；身份、教育、经历类的会入库。

### 2026-09-05-resume-parse-project-as-company · [通用] 简历自动解析把「项目名」填进公司名称

**症状**：上传简历后表单自动填好，但**工作经历的公司名称 = "PHMForge"、职位名称 = 研究助理**。
PHMForge 是论文/项目名，真正的机构是 IBM Research。
同批还错了：学院名称被填成 "ColumbiaUniversity"（校名塞进学院栏）、工作起始月份错、硕士就读时间为空。

**原因**：简历里 R1 那段的标题是「PHMForge — IBM Research 纽约」，解析器取了破折号前半段。
**这是商汤那次「PHMForge 被填进实习经历」的同类错误**——解析器分不清项目名和公司名。

**解法**：
**简历自动解析的结果一律当草稿，逐字段和 `candidate_profile.json` 对一遍再说**，
尤其这几个高危位：公司名称 / 学院名称 / 所有起止时间。
自动解析省的是打字，不是核对。日期字段还常常是空的或错位的，别以为"填上了就是对的"。

### 2026-09-05-resume-parse-ghost-entry · [通用] 简历解析凭空造出一段垃圾经历

**症状**：Moka 上传简历自动解析后，项目经验里多出一段：
项目名称 = 「后端开发」（那其实是某段实习的**职位名**），职责为空，
项目描述是**该段实习描述的后半截**——而前半截已经正确填进了实习经历。
同批还出现：某项目起止时间 `2026-9 ~ 2026-9`，而简历原文根本没写月份。

**原因**：解析器按换行/项目符号切段，一段经历被拦腰截断成两条；
日期缺失时它会**编一个**而不是留空。

**解法**：
1. **解析完必须数一遍条目数**，和简历实际段落数对得上再说。多出来的十有八九是拦腰截断。
2. 判断垃圾条目：**名称栏填的是职位名/公司名而不是项目名**，且内容和另一条重复。
   删除方式：Moka 每条右上角有「删除本条」，`scrollIntoView` 可能失效，
   用 JS 派发 `pointerdown/mousedown/mouseup/click` 四连点。
3. **简历没写月份的，解析器给的月份一律不可信**。回 `candidate_profile.json` /
   `experience_bank.md` 找依据；找不到就当推断处理并写进推断清单，别当事实。

### 2026-09-06-gitignore-dir-negation · [通用] .gitignore 里目录被排除后，里面的 ! 取反永远不生效

**症状**：`.gitignore` 写了

```
profile/
!profile/README.md
!profile/*.example.json
```

`git add -A` 之后 `profile/README.md` 和模板文件**一个都没被加进来**，也不报错。

**原因**：git 的规则是——**一个目录被排除后，git 根本不会进去遍历**，
所以目录内文件的取反规则永远不会被求值。这是 gitignore 的经典坑，不是 bug。

**解法**：把目录规则从 `dir/` 改成 `dir/*`：

```
profile/*
!profile/README.md
!profile/*.example.json
```

`dir/*` 排除的是「目录内的每个条目」而不是「目录本身」，git 会进去遍历，取反才有机会生效。

**验证方式**（别靠肉眼看 .gitignore）：
```bash
git add -A && git status --porcelain     # 看模板到底进没进来
git check-ignore -q <真实PII文件> && echo 挡住 || echo 泄漏   # 逐个确认该挡的还挡着
git ls-tree -r --name-only origin/main   # 推送后看远端实际收到什么
```

⚠️ 改完取反规则**必须两头都验**：模板进来了 **且** 真文件还挡着。
只验一头很容易把 PII 放出去。
