# 投后管道 —— 模板

`pipeline.md` 含真实投递史（哪家面到第几轮、哪家放弃、内推码是否被拒），
**已在 `.gitignore` 排除**。clone 后照这个格式自己建一份。

## 待办（按最近的时限排）

| 公司 | 岗位 | 投递日 | 下一步 | 时限ISO | 状态 | 备注 |
|---|---|---|---|---|---|---|
| 示例公司 | 示例岗位 | 2026-01-01 | 用户提交 | 2026-01-15 | 填完未提交 | 无暂存按钮，别关标签页 |

> 状态取值：`填完未提交` `已暂存` `已提交` `笔试中` `测评中` `面试中` `oc` `挂` `无音讯`
> **别把「投了」当终点** —— 停在提交前的，用户不点就永远不算投出去。

## 各公司投递规则（投前必查）

散落在 `platforms/` 各文件里的上限规则集中到这，因为投之前不会有人翻遍所有平台档案。

| 公司 | 上限 | 志愿数 | 来源 |
|---|---|---|---|
| 示例公司 | 2 个岗位 | 投递顺序=志愿顺序 | `platforms/xxx.md` |

## 同公司冷却期

**同一家公司 15 天内不重复投**（除非用户明确要求）。
短期多次投递会被 HR 系统标记，且多数公司的投递上限按批次算。

查法：本表 + `session-log/` 文件名日期。

## 「已提交」的判定

**看到页面出现可见的成功提示**（「投递成功」「已提交」「感谢投递」）才算。
填完表没看到确认 = 未提交。避免账本虚高。

## 时限从哪来

- 笔试/测评邀请通常发**邮件 + 短信**，agent 看不到 → 用户把邮件贴过来，agent 抽取时限填进上表
- **时限只填 ISO 日期，不填「还剩几天」**（见 `hunting/README.md` 2.8）
